import { createSplitLayout } from "../../layout/createSplitLayout";

const ReportsLayout = createSplitLayout({
  parentPath: "/reports",
  rootLabel: "Reports",
  defaultId: "all",
});

export default ReportsLayout;
