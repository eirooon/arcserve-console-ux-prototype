import { createSplitLayout } from "../../layout/createSplitLayout";

const DestinationsLayout = createSplitLayout({
  parentPath: "/destinations",
  rootLabel: "Destinations",
  defaultId: "all",
});

export default DestinationsLayout;
